import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rosagame.app',
  appName: 'com.rosagame.id',
  webDir: '.vercel/output/static'
};

export default config;
